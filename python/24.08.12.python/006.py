# 예약어(키워드) : 언어에서 사용하는 단어. 이름으로 사용할 수 없다

# 변수의 선언 : 변수를 만들어라
# 변수의 정의(초기화)

# let a;            JS에서 변수 선언
# a=10;
# JS는 선언과 정의가 분리되어 있다

# 파이썬은 선언과 동시에 정의해야 한다
a = 10
a = 3*5
print(a)

# 만일 print라는 함수를 지정해버린다면
print = 50
print(a) # 이 때 print는 더이상 함수가 아닌 변수이기때문에 에러가 난다
# 즉 많이 사용하는 단어들은 피해서 사용해야 한다